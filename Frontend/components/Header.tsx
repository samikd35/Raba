"use client"

export function Header() {
  return (
    <header className="sticky top-0 z-30 backdrop-blur supports-[backdrop-filter]:bg-white/60 dark:supports-[backdrop-filter]:bg-black/30 border-b border-[var(--border)]">
      <div className="h-16 flex items-center justify-end px-4">
        {/* Header content can be added here if needed */}
      </div>
    </header>
  )
}

